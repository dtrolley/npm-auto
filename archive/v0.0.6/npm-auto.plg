#!/bin/bash
# npm-auto.plg installer wrapper for Community Applications
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -d "${DIR}/package" ]; then
  echo "Running bundled install..."
  bash "${DIR}/package/install.sh"
else
  echo "No package directory found."
fi
